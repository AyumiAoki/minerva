<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.minerva">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.activities.TermoUsoActivity"></activity>
        <activity android:name=".ui.activities.MatematicaActivity" />
        <activity android:name=".ui.activities.LembreteActivity" />
        <activity android:name=".ui.activities.CadastroAgendaActivity" />
        <activity android:name=".ui.activities.ConteudoActivity"/>
        <activity android:name=".ui.activities.LinguagemCodigoActivity" />
        <activity android:name=".ui.activities.CienciasNaturezaActivity" />
        <activity android:name=".ui.activities.NotaActivity" />
        <activity android:name=".ui.activities.IndustrializacaoActivity" />
        <activity android:name=".ui.activities.ConteudoGeografiaActivity" />
        <activity android:name=".ui.activities.CienciasHumanasActivity" />
        <activity android:name=".ui.activities.CadastroNotaActivity" />
        <activity android:name=".ui.activities.IntroducaoActivity" />
        <activity android:name=".ui.activities.ApresentacaoActivity" />
        <activity android:name=".ui.activities.RecuperarSenhaActivity" />
        <activity android:name=".ui.activities.CadastroActivity" />
        <activity android:name=".ui.activities.MainActivity" />
        <activity android:name=".ui.activities.LoginActivity" />
        <activity android:name=".ui.activities.TelaInicialActivity" />
        <activity
            android:name=".ui.activities.SplashScreenActivity"
            android:theme="@style/AppCompat.TelaCheia">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <receiver android:name=".util.MyNotificationSystem" />

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>